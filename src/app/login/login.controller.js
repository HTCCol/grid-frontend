(function() {
    'use strict';

    angular.module('hpc.login')
        .controller('LoginController', LoginController);

    /* @ngInject */
    function LoginController($scope, jwt_loginService, $location) {
               
    }
})();
